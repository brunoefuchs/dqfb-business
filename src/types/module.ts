export interface ModuleConfig {
  id: string;
  title: string;
  subtitle: string;
  label: string;
  description: string;
  icon: string;
  iconFilled?: boolean;
  bgColor: string;
  buttonText: string;
  buttonTextColor: string;
  url: string;
}
