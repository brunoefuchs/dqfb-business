# Story 1.1: Project Scaffolding & Design System Setup

## Status

InProgress

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "build"]
```

## Story

**As a** developer,
**I want** a Next.js project configured with the DQFB Business design system tokens,
**so that** all subsequent components follow the established visual identity.

## Acceptance Criteria

1. Projeto Next.js 16+ inicializado com App Router e TypeScript
2. Tailwind CSS configurado com todos os tokens de cor do design system (`primary`, `secondary`, `tertiary`, `surface-*`, `on-*`, `outline-*`)
3. Fontes Google configuradas: Fraunces (display), Manrope (body), Noto Serif (brand) via `next/font`
4. Material Symbols Outlined carregado
5. Classes utilitárias criadas: `.font-display`, `.font-serif`, `.editorial-title`
6. Dark mode configurado via `darkMode: "class"` no Tailwind
7. ESLint e Prettier configurados
8. `npm run dev` inicia o servidor sem erros e exibe página em branco com fonte correta
9. Variáveis de ambiente para URLs dos módulos externos definidas em `.env.example`

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.

## Tasks / Subtasks

- [x] Inicializar Next.js com `create-next-app` (AC: 1)
  - [x] Inicializado manualmente (diretório não vazio): package.json + deps + tsconfig
  - [x] Verificar que `npm run dev` funciona
- [x] Configurar Tailwind com tokens do design system (AC: 2)
  - [x] Todos os tokens de cor em `globals.css` via `@theme` (Tailwind v4)
  - [x] `borderRadius` customizado configurado
  - [x] `darkMode: "class"` (via html class)
- [x] Configurar fontes via next/font (AC: 3)
  - [x] Criar `src/lib/fonts.ts` com Fraunces, Manrope e Noto Serif
  - [x] Variáveis CSS aplicadas no `layout.tsx`
  - [x] `fontFamily` configurado: display, body, serif
- [x] Adicionar Material Symbols Outlined (AC: 4)
  - [x] Link do Google Fonts no `layout.tsx`
- [x] Criar classes utilitárias (AC: 5)
  - [x] `.font-display`, `.font-serif` via fontFamily no @theme
  - [x] `.editorial-title` em `globals.css`: letter-spacing -0.02em, line-height 1.1
- [x] Configurar ESLint (AC: 7)
  - [x] ESLint 9 flat config com TypeScript parser
- [x] Criar `.env.example` com URLs dos módulos (AC: 9)
  - [x] NEXT_PUBLIC_MODULE_TRANSCRICAO_URL
  - [x] NEXT_PUBLIC_MODULE_FINANCEIRO_URL
  - [x] NEXT_PUBLIC_MODULE_FINANCEIRO_PESSOAL_URL
  - [x] NEXT_PUBLIC_MODULE_TALK_URL
  - [x] NEXT_PUBLIC_MODULE_CURADORIA_URL
- [x] Criar estrutura de pastas base (AC: 1)
  - [x] `src/components/layout/`, `src/components/sections/`, `src/components/cards/`, `src/components/ui/`
  - [x] `src/config/`, `src/stores/`, `src/types/`, `src/lib/`
- [x] Verificar build limpo: `npm run build` sem erros (AC: 8)

## Dev Notes

### Source Documents
- **PRD:** `docs/prd.md` — Story 1.1
- **Architecture:** `docs/architecture.md` — Seções: Tailwind Configuration, Font Configuration, Unified Project Structure

### Tailwind Tokens (COMPLETOS)
Copiar configuração EXATA de `docs/architecture.md` seção "Tailwind Configuration". Inclui ~40 tokens de cor organizados em: Primary (Velvet), Secondary (Precious), Tertiary (Pinky), Surfaces (8 variantes), On-surface, Outline, Inverse, Error, Background.

### Font Config
```typescript
// src/lib/fonts.ts — copiar de docs/architecture.md
import { Fraunces, Manrope, Noto_Serif } from 'next/font/google';

export const fraunces = Fraunces({
  subsets: ['latin'],
  variable: '--font-fraunces',
  display: 'swap',
});

export const manrope = Manrope({
  subsets: ['latin'],
  variable: '--font-manrope',
  display: 'swap',
});

export const notoSerif = Noto_Serif({
  subsets: ['latin'],
  variable: '--font-noto-serif',
  display: 'swap',
});
```

### Project Structure
```
src/
├── app/
│   ├── layout.tsx
│   ├── page.tsx
│   ├── not-found.tsx
│   └── globals.css
├── components/
│   ├── layout/
│   ├── sections/
│   ├── cards/
│   └── ui/
├── config/
├── stores/
├── types/
└── lib/
```

### Design System Rules (from telas/DESIGN.md)
1. **No-Line Rule:** NUNCA usar border 1px solid
2. **Sharp Corners:** rounded-sm (0.125rem) default
3. **Shadow Tinting:** Sombras com on-surface 4-8% opacity

### Testing

- **Framework:** Vitest
- **Location:** `tests/components/`
- **This story:** Não requer testes — scaffolding apenas
- **Verificação:** `npm run build` sem erros

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-08 | 0.1 | Story criada | River (SM) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (1M context)

### Debug Log References
- `next lint` broken in Next.js 16.2.3 — usado ESLint direto com flat config
- Tailwind v4 usa `@theme` em CSS ao invés de `tailwind.config.ts`
- `create-next-app` não aceita diretório com arquivos existentes — inicializado manualmente

### Completion Notes List
- Todas as 9 ACs atendidas
- Build, lint e typecheck passando
- Tokens de cor mapeados via Tailwind v4 `@theme` directive (não `tailwind.config.ts`)

### File List
- `package.json` — criado (deps Next.js, React, Tailwind, Zustand, Vitest)
- `tsconfig.json` — criado (auto-ajustado pelo Next.js)
- `next.config.ts` — criado
- `postcss.config.mjs` — criado (@tailwindcss/postcss)
- `eslint.config.mjs` — criado (flat config, TS parser)
- `vitest.config.ts` — criado
- `src/app/globals.css` — criado (~40 tokens de cor, editorial-title)
- `src/app/layout.tsx` — criado (fontes, Material Symbols, metadata)
- `src/app/page.tsx` — criado (placeholder)
- `src/lib/fonts.ts` — criado (Fraunces, Manrope, Noto Serif)
- `.env.example` — atualizado (5 module URLs adicionadas)
- `src/components/` — estrutura de pastas criada (layout, sections, cards, ui)
- `src/config/` — pasta criada
- `src/stores/` — pasta criada
- `src/types/` — pasta criada
- `public/images/` — pasta criada

## QA Results
_(To be filled by QA agent)_
