# Story 1.6: Dark Mode & Responsive Polish

## Status

Ready

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "build"]
```

## Story

**As a** user,
**I want** the portal to work flawlessly on all devices and support dark mode,
**so that** I have a consistent premium experience regardless of context.

## Acceptance Criteria

1. Dark mode toggle funcional — alterna classe `dark` no HTML root
2. Cores dark mode aplicadas: backgrounds `stone-950`/`stone-900`, textos `#EDD4D7`, links `#CE3B87`
3. Cards mantêm cores próprias em dark mode (são sempre escuros)
4. Mobile: navegação oculta, cards em 1 coluna, hero em font 6xl → 4xl, citação em stack vertical
5. Tablet: cards em 2 colunas, hero mantém proporção
6. Sem scroll horizontal em nenhum breakpoint
7. Todas as transições (hover cards, grayscale imagem, links) funcionam em touch devices
8. Lighthouse performance score >= 90 em desktop

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled

## Tasks / Subtasks

- [ ] Criar Zustand store `src/stores/theme.ts` (AC: 1)
  - [ ] State: `isDark: boolean`, action: `toggle()`
  - [ ] Copiar implementação de `docs/architecture.md` seção "State Management"
- [ ] Criar componente `src/components/ui/DarkModeToggle.tsx` (AC: 1)
  - [ ] Botão que chama `useThemeStore().toggle()`
  - [ ] Aplica/remove classe `dark` no `document.documentElement`
  - [ ] Ícone: `light_mode` / `dark_mode` (Material Symbols)
- [ ] Integrar DarkModeToggle no Header (AC: 1)
- [ ] Verificar todas as cores dark mode em cada componente (AC: 2)
  - [ ] Header: `dark:bg-stone-950`, logo `dark:text-[#EDD4D7]`
  - [ ] Divisores: `dark:bg-stone-900`
  - [ ] Footer: `dark:bg-stone-950`
  - [ ] Links: `dark:text-stone-400`, `dark:text-[#CE3B87]`
- [ ] Verificar que cards NÃO mudam em dark mode (AC: 3)
- [ ] Ajustar responsive mobile (AC: 4)
  - [ ] Hero: `text-4xl sm:text-6xl md:text-7xl`
  - [ ] Quote section: stack vertical em mobile (grid-cols-1)
  - [ ] Verificar `max-w-[1920px]` no main
- [ ] Ajustar responsive tablet (AC: 5)
  - [ ] Cards: `sm:grid-cols-2 lg:grid-cols-5`
- [ ] Testar scroll horizontal (AC: 6)
  - [ ] Verificar `overflow-x-hidden` no body
- [ ] Verificar hover em touch (AC: 7)
- [ ] Rodar Lighthouse e garantir score >= 90 (AC: 8)

## Dev Notes

### Source Documents
- **PRD:** `docs/prd.md` — NFR1, NFR4
- **Architecture:** `docs/architecture.md` — State Management, Performance
- **Prototype:** `telas/code.html` — dark mode classes já presentes no HTML

### Zustand Store
```typescript
// src/stores/theme.ts
import { create } from 'zustand';

interface ThemeStore {
  isDark: boolean;
  toggle: () => void;
}

export const useThemeStore = create<ThemeStore>((set) => ({
  isDark: false,
  toggle: () => set((state) => ({ isDark: !state.isDark })),
}));
```

### Dark Mode Token Mapping
- `bg-background` → já é `#fcf8f7` light, precisa de dark variant
- `text-on-surface` → já é `#1c1b1b` light
- O protótipo usa `dark:bg-stone-950`, `dark:text-[#EDD4D7]` — seguir esse padrão

### Breakpoints (Tailwind defaults)
- `sm`: 640px (2 colunas cards)
- `md`: 768px (nav visível, grid 12 cols)
- `lg`: 1024px (5 colunas cards)

### Design System Rules
- **Overflow:** Body deve ter `overflow-x-hidden` para prevenir scroll horizontal
- Cards já são escuros — dark mode não os afeta

### Dependencies
- Requer Stories 1.1 a 1.5 completas (todos os componentes)

### Testing

- **Framework:** Vitest + Playwright
- **Unit tests:** `tests/components/DarkModeToggle.test.tsx` — toggle muda estado
- **E2E tests:** `tests/e2e/portal.spec.ts` — responsividade em 3 viewports, dark mode toggle
- **Lighthouse:** Executar manualmente ou via CI

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-08 | 0.1 | Story criada | River (SM) |

## Dev Agent Record

### Agent Model Used
_(To be filled by dev agent)_

### Debug Log References
_(To be filled by dev agent)_

### Completion Notes List
_(To be filled by dev agent)_

### File List
_(To be filled by dev agent)_

## QA Results
_(To be filled by QA agent)_
