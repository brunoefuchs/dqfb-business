# Story 1.7: Deploy & Go-Live

## Status

InReview

## Executor Assignment

```yaml
executor: "@devops"
quality_gate: "@architect"
quality_gate_tools: ["build", "lighthouse"]
```

## Story

**As a** owner,
**I want** the portal deployed and accessible via URL pública,
**so that** I can acessar meus sistemas de qualquer lugar.

## Acceptance Criteria

1. Aplicação deployada na Vercel com domínio funcional
2. Variáveis de ambiente dos módulos configuradas na Vercel
3. HTTPS ativo
4. Build sem erros ou warnings
5. Todas as fontes carregando corretamente (Google Fonts)
6. Links dos 5 módulos redirecionando para as URLs corretas dos sistemas externos
7. Responsividade verificada em dispositivo real (mobile)

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled

## Tasks / Subtasks

- [ ] Criar GitHub Actions workflow `.github/workflows/deploy.yml` (AC: 4)
  - [ ] Jobs: lint, typecheck, test, build
  - [ ] Copiar pipeline de `docs/architecture.md` seção "CI/CD Pipeline"
- [ ] Conectar repositório à Vercel (AC: 1)
  - [ ] `vercel link` ou via dashboard Vercel
  - [ ] Configurar build command: `next build`
  - [ ] Configurar output directory: `.next`
- [ ] Configurar variáveis de ambiente na Vercel (AC: 2)
  - [ ] NEXT_PUBLIC_MODULE_TRANSCRICAO_URL
  - [ ] NEXT_PUBLIC_MODULE_FINANCEIRO_URL
  - [ ] NEXT_PUBLIC_MODULE_FINANCEIRO_PESSOAL_URL
  - [ ] NEXT_PUBLIC_MODULE_TALK_URL
  - [ ] NEXT_PUBLIC_MODULE_CURADORIA_URL
- [ ] Verificar HTTPS (AC: 3)
  - [ ] Vercel provisiona SSL automaticamente
- [ ] Executar build de produção (AC: 4)
  - [ ] `npm run build` sem erros
- [ ] Verificar fontes carregando (AC: 5)
  - [ ] Fraunces, Manrope, Noto Serif via next/font
- [ ] Testar todos os 5 links de módulos (AC: 6)
- [ ] Testar em dispositivo mobile real (AC: 7)
- [ ] Push final via @devops

## Dev Notes

### Source Documents
- **PRD:** `docs/prd.md` — Story 1.7
- **Architecture:** `docs/architecture.md` — Deployment Architecture, CI/CD Pipeline

### CI/CD Pipeline (copiar de architecture.md)
```yaml
name: Deploy
on:
  push:
    branches: [main]
  pull_request:

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npx tsc --noEmit
      - run: npm test
      - run: npm run build
```

### Vercel Config
- Framework preset: Next.js (auto-detected)
- Build command: `next build`
- Output: `.next`
- Root: `.` (raiz do repo)
- Node.js version: 20.x

### IMPORTANTE
- Esta story é executada pelo @devops (Gage) — ÚNICO agente autorizado para push e deploy
- URLs dos módulos devem ser fornecidas pelo owner antes do deploy
- Se URLs ainda não existem, usar placeholders (#) e atualizar depois

### Dependencies
- Requer Stories 1.1 a 1.6 completas
- Requer URLs dos sistemas externos do owner

### Testing

- **Verificação manual:** Abrir URL da Vercel em desktop e mobile
- **CI:** GitHub Actions valida build antes do merge
- **Lighthouse:** Score >= 90 em produção

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-08 | 0.1 | Story criada | River (SM) |

## Dev Agent Record

### Agent Model Used
_(To be filled by dev agent)_

### Debug Log References
_(To be filled by dev agent)_

### Completion Notes List
_(To be filled by dev agent)_

### File List
_(To be filled by dev agent)_

## QA Results
_(To be filled by QA agent)_
